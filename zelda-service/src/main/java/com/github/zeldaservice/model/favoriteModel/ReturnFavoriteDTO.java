package com.github.zeldaservice.model.favoriteModel;

public interface ReturnFavoriteDTO {
    // interfaces não precisam (nem devem) ter fields públicos. Eles SEMPRE são públicos.
    // Não faz sentido uma interface com campo private.

    String getDescription();
    public String getGame();
    public String getGame_id();


// espaços, espaços, ESPAÇOS!

}
